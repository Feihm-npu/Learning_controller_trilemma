# Stage-1 问题精确化：学习型工业控制器的 safety-security-learning 三难

> 状态：stage-1 初稿（2026-06-30）。目的不是给技术路线，而是把 round-6 的开放判断收缩成可核验、可证伪、可进入 stage-3 可行性分析的研究命题。

## 1. 一句话头条

在线更新的学习型工业控制器在过程感知的策略性 FDI/传感器欺骗对手下，能否持续持有闭环物理安全证书？如果不能，safety（固定保守证书）、security（对抗策略性欺骗）、learning（部署后更新）三者之间的不可兼得边界是什么？

## 2. 精确问题

给定工业过程 $P$、学习型控制器族 $\pi_\theta$、在线更新规则 $U$、安全集合 $S$ 与攻击者族 $\mathcal{A}$，是否存在一个认证机制 $\mathsf{Cert}$，使得控制器在部署后持续更新时仍满足：

$$
\forall a \in \mathcal{A},\ \forall t,\quad x_t \in S
$$

并且该保证不是仅对有界扰动、固定控制器或离线一次性验证成立，而是对会利用过程模型、传感器通道和更新数据的策略性 security 对手成立。

可证伪目标分两类：

1. 可能性框架：给出一组非平凡条件，使在线更新控制器在策略性 FDI 下可以维持、刷新或撤销闭环安全证书。
2. 不可能性边界：证明在某些自然条件下，任意机制无法同时满足持续学习、策略性欺骗安全和闭环 safety certificate。

## 3. 最小系统模型

### 3.1 物理过程

离散时间闭环系统：

$$
x_{t+1} = f(x_t, u_t, w_t), \quad y_t = h(x_t) + v_t + a_t
$$

其中 $x_t$ 是真实物理状态，$u_t$ 是控制输入，$w_t$ 是过程扰动，$v_t$ 是传感噪声，$a_t$ 是攻击者注入的虚假传感/观测数据。工业对象可以先从低维线性系统、微电网频率控制、机器人轨迹跟踪或水箱/化工 benchmark 开始。

### 3.2 学习型控制器

控制器为 $u_t=\pi_{\theta_t}(\hat{x}_t, c_t)$，其中 $\theta_t$ 会在部署后由更新规则改变：

$$
\theta_{t+1}=U(\theta_t, D_t, r_t, \mathcal{C}_t)
$$

这里 $D_t$ 可包含传感历史、奖励/代价、运行日志或人工/LLM-in-loop 建议；$\mathcal{C}_t$ 是当前证书或安全约束。关键是 $\pi_{\theta_t}$ 的 action map 会变，不只是状态估计器或检测器更新。

### 3.3 安全证书

闭环安全证书可以实例化为下列任一种：

1. 正不变集 $I \subseteq S$：若 $x_t\in I$，则所有被允许扰动/攻击下 $x_{t+1}\in I$。
2. 控制屏障函数 $B(x)$：满足 $B(x)\ge 0$ 的安全域在闭环下保持。
3. 可达性证书：从初始集 $X_0$ 出发，有限/无限时域 reachable set 不触碰 unsafe set。
4. 运行时 shield/safety filter：当学习控制器动作可能违反证书时接管或投影。

本方向的“证书”必须约束真实状态 $x_t$，不能只证明观测扰动下动作不变、Q value 鲁棒或分类输出稳定。

### 3.4 策略性 security 对手

攻击者不是普通有界扰动，而是过程感知 FDI/欺骗者：

1. 知道或估计 $f,h,\pi,U,\mathsf{Cert}$。
2. 可操控部分传感器、通信数据、奖励信号或训练日志。
3. 选择多步策略 $a_{0:T}$，目标是在保持隐蔽或过程一致的同时，使真实状态离开 $S$ 或使证书错误维持。
4. 受非平凡限制：攻击面、注入幅度、频率、能量、传感器子集或 stealthiness 预算有限；否则问题会退化为平凡不可能。

建议先把 $\mathcal{A}$ 拆成三层：

| 层级 | 攻击能力 | 用途 |
|---|---|---|
| A1 bounded observation perturbation | 每步观测扰动有范数界 | 对照已有 adversarial RL/safety filter |
| A2 process-consistent FDI | 注入序列需与某个可行物理轨迹一致 | 体现 CPS/ICS security |
| A3 learning-aware FDI | 注入还会操控控制器更新数据或奖励 | 触发三难核心 |

真正的开放核应落在 A2/A3，而不是 A1。

## 4. 三难的可操作定义

### Safety

存在可检查的闭环证书 $\Gamma_t$，证明真实物理状态保持在安全集合 $S$ 中。证书应依赖闭环动态 $(f,\pi_{\theta_t})$，而不是只依赖单步感知鲁棒性。

### Security

保证对策略性攻击族 $\mathcal{A}$ 成立，特别是 FDI/传感器欺骗可让控制器和学习规则看到与真实状态不同但过程一致的数据。

### Learning

部署后 $\theta_t$ 允许更新，并且更新对控制策略有实质影响。若只允许固定控制器或完全冻结参数，则退回传统认证；若每次更新都完整重认证且停机，则工程意义弱。

### 三难版本

一个机制若同时满足以下三项，则解决三难：

1. Nontrivial learning：控制器可在部署后根据新数据改善或适应，且 action map 发生非零变化。
2. Certified safety：每次控制动作或每个更新周期都有针对真实状态的闭环安全证书。
3. Strategic security：证书在 A2/A3 类过程感知欺骗对手下仍有效，或能及时撤销/冻结以避免 unsafe。

不可能性版本则证明：在给定攻击面、观测不可区分性或更新规则依赖被污染数据的条件下，任意机制至多满足其中两项。

## 5. 非目标和边界

本方向不做以下问题：

1. 纯 ICS/智能电网异常检测。zero-dynamics/隐蔽攻击检测边界已有经典结果，本项目已剔除该坑。
2. 固定控制器在有界扰动下的可达性验证。这是必要背景，不是 gap。
3. 只证明 safe-RL 可被攻击。已有 backdoor/safe-RL CPS 攻击工作，不能作为主贡献。
4. 只做范数扰动下的 RL certified robustness。它不等价于过程感知 security。
5. 只做工程认证流程综述。NASA/adaptive flight control 已说明认证缺口，但没有 security 三难。

## 6. 候选核心命题

### 命题 A：证书生命周期不守恒

即使 $\pi_{\theta_t}$ 在时刻 $t$ 对标称闭环有安全证书，若更新规则 $U$ 使用可被 FDI 污染的部署数据，则 $\theta_{t+1}$ 的证书一般不随更新保持。需要额外的 certificate-preserving update 约束，否则安全证书会在学习算子下失效。

适合形态：保持定理/反例/最小系统证明。

### 命题 B：可信状态缺失下的不可能性

若攻击者能构造两个对控制器和更新规则不可区分的观测历史，但对应真实状态一个安全、一个不安全，则任何只基于该历史更新和认证的机制无法同时保证持续学习和真实状态安全。

适合形态：不可区分性/no-go theorem。

### 命题 C：可行性边界

若存在可信最小传感子集、更新幅度界、证书增量验证和运行时 shield，则可以维持一个保守安全核；超出该条件，必须冻结学习、撤销证书或降级到安全控制器。

适合形态：条件性可能框架 + toy theorem。

## 7. 最小 hero example

优先从低维线性系统做反例/边界：

$$
x_{t+1}=Ax_t+Bu_t,\quad y_t=x_t+a_t
$$

控制器 $u_t=K_t y_t$，在线更新 $K_{t+1}=U(K_t, y_{0:t}, u_{0:t})$，安全集合 $S=\{x: |x|\le c\}$。攻击者选择过程一致的 $a_t$，使控制器看到的 $y_t$ 留在安全域内，但真实 $x_t$ 逐步漂移到 unsafe，或让 $U$ 学到更激进的 $K_{t+1}$，使原证书失效。

目标展示三件事：

1. 固定 $K$ 或有界扰动模型下的 safety certificate 可以成立。
2. 在线更新后，旧证书不自动保持。
3. 策略性 FDI 可以同时污染控制动作和更新数据，使“看似安全”的证书与真实状态脱钩。

## 8. Stage-3 进入条件

进入可行性阶段前，需要完成：

1. 选定 A/B/C 中一个主命题，避免同时做 no-go 和完整框架。
2. 固定攻击者族 $\mathcal{A}$，尤其是 stealth/process-consistency 限制。
3. 固定证书类型，优先从 barrier/invariant set 二选一。
4. 给出一个 2D 或低维线性系统上的手算反例或 toy theorem。
5. 对近邻工作完成 gap matrix，证明没有直接覆盖三元组合。

## 9. Kill criteria

以下任一成立，应降级或转向：

1. 找到已有工作同时覆盖在线更新学习控制器、策略性 FDI/欺骗对手和闭环安全证书。
2. 只能证明“攻击者任意改所有传感器所以安全不可能”的平凡结论。
3. 只能做异常检测准确率或 attack success rate，没有证书生命周期/不可能边界。
4. hero example 无法区别有界扰动 adversary 与过程感知 FDI。
5. 形式化只停留在 position/SoK，无法给出 theorem、反例或可执行框架。
